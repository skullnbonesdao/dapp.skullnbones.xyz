import { defineStore } from 'pinia';

export const useAtlasLockerStore = defineStore('atlasLockerStore', {
  state: () => ({}),

  getters: {},
  actions: {
    async updateStore() {
      return;
    },
  },
});
